# Todo
- [ ] Kea integration
- [ ] Unifi integration
- [ ] Proxmox API Integration - importing devices
- [ ] Add separate develop docker image
- [ ] Add customizable roles (from OIDC) to the dashboard services. Consider if Authentik integration could be used.
- [x] Add checks to prevent overwriting docker x.x.x images
- [x] Add husky or prek and run commands like cargo fmt before commit
- [x] Add `compose.yml` version update to `bump-version` script. Compose should use explicit x.x version tag to prevnt breaking changes.
